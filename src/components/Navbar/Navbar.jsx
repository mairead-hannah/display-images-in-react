import React, { Component } from 'react';
import styles from "./Navbar.module.scss"


export default class Navbar extends Component {
  state = {  }
  render() { 
    return (
      <nav>
        <p>to add later - burger menu with font awesome</p>
      </nav>
    );
  }
}

